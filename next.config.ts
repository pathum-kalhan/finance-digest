import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["image.cnbcfm.com","static2.finnhub.io","data.bloomberglp.com"],
  },
};

export default nextConfig;
